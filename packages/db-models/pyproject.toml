[project]
name = "db-models"
version = "0.1.0"
description = "Database models package"
requires-python = ">=3.11,<3.12"
dependencies = [
    "sqlalchemy>=2.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/db_models"]
