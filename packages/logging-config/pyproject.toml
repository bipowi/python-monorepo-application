[project]
name = "logging-config"
version = "0.1.0"
description = "Logging configuration package"
requires-python = ">=3.11,<3.12"
dependencies = [
    "structlog>=24.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/logging_config"]
