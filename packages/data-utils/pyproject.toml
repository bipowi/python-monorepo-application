[project]
name = "data-utils"
version = "0.1.0"
description = "Data utility package"
requires-python = ">=3.11,<3.12"
dependencies = [
    "pandas>=2.0",
    "numpy>=1.24",
]

[tool.hatch.build.targets.wheel]
packages = ["src/data_utils"]
